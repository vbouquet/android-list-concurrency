<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fr.parisnanterre.pmoo.androidm2td1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.ListFilmAsyncTaskActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.ListFilmThreadActivity" />
        <activity android:name=".activity.ListFilmExecutorActivity" />
        <activity android:name=".activity.ListFilmThreadPoolExecutorActivity"/>

        <meta-data android:name="DATABASE" android:value="films.db" />
        <meta-data android:name="VERSION" android:value="3" />
        <!--<meta-data android:name="DOMAIN_PACKAGE_NAME" android:value="fr.parisnanterre.pmoo.androidm2td1" />-->
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>
